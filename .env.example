# Example .env file. DO NOT commit your secrets to Git.
# Copy this to `.env` and set values locally.

# PyPI API token for publishing with twine (used for local publish only).
# Set this token in your GitHub repository `Settings -> Secrets` as PYPI_API_TOKEN for CI releases.
PYPI_API_TOKEN=your-pypi-token-here
