// {{ pascal_name }} Agent
// Generated by restack-gen on {{ now().strftime('%Y-%m-%d') }}

import { agent, log } from "@restackio/ai/agent";

@agent.defn()
export class {{ pascal_name }}Agent {
  @agent.event()
  async {{ snake_name }}Event(event: any): Promise<any> {
    log.info("{{ pascal_name }}Agent received event", { event });
    return { status: "processed", agent: "{{ pascal_name }}" };
  }

  @agent.run()
  async run(inputData: any): Promise<any> {
    log.info("{{ pascal_name }}Agent starting", { input: inputData });

    // Agent logic here
    const result = {
      message: "Hello from {{ pascal_name }}Agent",
      input: inputData,
      agent: "{{ pascal_name }}"
    };

    log.info("{{ pascal_name }}Agent completed", { result });
    return result;
  }
}

// For local development
if (require.main === module) {
  const agentInstance = new {{ pascal_name }}Agent();
  agentInstance.run({ test: true }).then(result => {
    console.log("Agent result:", result);
  });
}