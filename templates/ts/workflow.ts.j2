// {{ pascal_name }} Workflow
// Generated by restack-gen on {{ now().strftime('%Y-%m-%d') }}

import { workflow, log } from "@restackio/ai/workflow";

@workflow.defn()
export class {{ pascal_name }}Workflow {
  @workflow.run()
  async run(inputData: any): Promise<any> {
    log.info("{{ pascal_name }}Workflow starting", { input: inputData });

    // Workflow logic here - can call functions and agents
    const result = {
      message: "Hello from {{ pascal_name }}Workflow",
      input: inputData,
      workflow: "{{ pascal_name }}"
    };

    log.info("{{ pascal_name }}Workflow completed", { result });
    return result;
  }
}

// For local development
if (require.main === module) {
  const workflowInstance = new {{ pascal_name }}Workflow();
  workflowInstance.run({ test: true }).then(result => {
    console.log("Workflow result:", result);
  });
}