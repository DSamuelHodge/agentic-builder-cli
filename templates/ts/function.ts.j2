/* Function template (TypeScript) */

import type { FunctionContext } from "../types.js";

export interface {{ pascal_name }}Input {
  // shape your function inputs here
  value: string;
}

export interface {{ pascal_name }}Output {
  // shape your function outputs here
  ok: boolean;
  result?: string;
}

export async function {{ name }}(
  input: {{ pascal_name }}Input,
  ctx: FunctionContext
): Promise<{{ pascal_name }}Output> {
  ctx.log.debug("Running function {{ name }} with:", input);
  try {
    // Business logic
    const result = `Processed: ${input.value}`;
    return { ok: true, result };
  } catch (err) {
    ctx.log.error("{{ name }} failed:", err);
    return { ok: false };
  }
}