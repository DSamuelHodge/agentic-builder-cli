
<!-- @generated by restack-gen v{{ generator_version }} ({{ timestamp }}) -->
# {{ project_name }}

This project was generated by Restack Generator CLI from a pipeline expression.

## Structure

```
{{ project_name }}/
├── src/
│   ├── agents/
│   ├── functions/
│   ├── workflows/
│   ├── types.ts
│   ├── services.ts
│   ├── client.ts
│   └── index.ts
├── tests/
├── package.json
├── tsconfig.json
├── .env.example
├── .gitignore
└── README.md
```

## Setup

Recommended: Use Node.js 20+ and npm 9+ for best compatibility.

```
# Install dependencies
npm install

# Build the project
npm run build
```

## Run

```
npm start
# or
node ./dist/client.js
```

## Services

Start services to register agents, functions, and workflows:

```
npm run services
# or
node -e "import('./dist/services.js').then(m => m.startServices())"
```
