<!-- @generated by restack-gen v{{ generator_version }} ({{ timestamp }}) -->

# {{ project_name }}

This project was generated by Restack Generator CLI from a pipeline expression.

## Structure

```
{{ snake_app_name }}/
├── {{ snake_app_name }}/
│   ├── agents/
│   ├── functions/
│   ├── workflows/
│   ├── types.py
│   ├── services.py
│   ├── client.py
│   └── __init__.py
├── tests/
├── requirements.txt
├── pyproject.toml
├── .env.example
├── .gitignore
└── README.md
```

## Setup

Recommended: Use Python 3.12 for best compatibility.

Supported Python versions: 3.10, 3.11, 3.12 — building on Python 3.13+ may fail for some dependencies (e.g., restack-ai).

```
# On Windows with py launcher (if Python 3.12 is installed):
py -3.12 -m venv .venv
.venv\\Scripts\\Activate.ps1
python -m pip install --upgrade pip wheel
python -m pip install -r requirements.txt

# Alternatively, use `uv` to create a virtual env and install deps:
uv venv
# Activate the environment (Windows):
.venv\\Scripts\\activate
# Activate the environment (Unix/macOS):
source .venv/bin/activate
uv pip install -r requirements.txt

# If you don't have Python 3.12, install it first:
# winget install -e --id Python.Python.3.12
# or download from https://www.python.org/downloads/release/python-3120/
```

## Code Quality

```bash
# Use uv run to execute tools installed in the virtual environment
uv run ruff check .
uv run ruff format .
uv run black --check .
# Security checks
uv run pip-audit
uv run bandit -r {{ package_name|default('src') }}
```

## Run

```
python -m {{ snake_app_name }}.client
```

## Services

Start services to register agents, functions, and workflows:

```
python -c "import asyncio; from {{ snake_app_name }}.services import start_services; asyncio.run(start_services())"
```